# Arm

The arm component owns transport-neutral joint motion and manipulation
contracts. `JointMotionProfile` produces smooth canonical commands.

Operator surfaces and ROS command adapters are nested under `adapters/ros2`.
MoveIt and cuMotion integrations belong under `providers/`.
